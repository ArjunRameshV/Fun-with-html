C - Assignment : 

divisible by 3 :

#include  <stdio.h>

int main(void)
{
int a,b ;
    scanf("%d",&a) ; 
    b = a ; 
    int sum = 0 ; 
    while(a>0){
    sum = sum + a%10 ; 
    int digit = a%10 ; 
    printf("%d\n",digit) ; 
    a = a/10 ;     
    }
    if(sum%3 == 0){
    printf("%d is divisible by 3.",b);
    } else {
    printf("%d is divisible by 3.",b) ; 
    }    
    return 0;
}

----------------------------------------
#include  <stdio.h>

int main(void)
{
int a,b ;
    scanf("%d",&a) ; 
    if(a%3 == 0){
        printf("%d is divisible by 3.",a);
    } else {
    printf("%d is not divisible by 3.",a);
    }
    return 0;
}

-----------------------------------------
#include  <stdio.h>

int main(void)
{
int a,b ;
    scanf("%d",&a) ; 
    b = a ; 
    while(a>0){
    a=a-3 ; 
    }
    if(a==0){
    printf("%d is divisible by 3.",b);
    } else {
    printf("%d is not divisible by 3.",b) ; 
    }
    
    return 0;
}
 
-------------------------------------------
Divisible by 4 : 

#include  <stdio.h>

int main(void)
{
int a,b ;
    scanf("%d",&a) ; 
    b = a ; 
    a = a%100 ; 
    if(a%4 == 0){
    printf("%d is divisible by 4",b);
    } else {
    printf("%d is not divisible by 4",b);
    }
    
    return 0;
}

------------------------------
#include  <stdio.h>

int main(void)
{
int a,b ;
    scanf("%d",&a) ;
    b = a ; 
    if (a%2 != 0){
    printf("%d is not divisible by 4",b);
    } else {
    a = a/2 ; 
        if(a%2 == 0){
            printf("%d is divisible by 4",b) ;
        } else {
        printf("%d is divisible by 4",b);
        }
    }
    return 0;
}

-----------------------------------
#include <stdio.h>

void main()
{
  int a,b ;
  scanf("%d",&a) ;
  b = a  ;
  a = a%100 ;
  int x,y ;
  x = a/10 ;
  y = a%10 ;
  if((2*x+y)%4 == 0){
    printf("%d is divisible by 4.\n",b);
  } else{
  printf("%d is not divisible by 4.\n",b);
  }

    }
